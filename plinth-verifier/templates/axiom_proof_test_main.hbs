{-# LANGUAGE NoImplicitPrelude #-}

module Main where

import qualified Generic.VerificationTestPlutus
import qualified Test.Tasty as Tasty
import Prelude (
    IO,
    ($),
 )

main :: IO ()
main =
    Tasty.defaultMain $
        Tasty.testGroup
            "Axiom SHPLONK verifier tests"
            [Generic.VerificationTestPlutus.test]
