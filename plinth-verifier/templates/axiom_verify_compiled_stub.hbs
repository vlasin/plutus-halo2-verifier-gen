{-# LANGUAGE NoImplicitPrelude #-}

module Generic.VerifyCompiled (
    verifyAppliedCompiled,
    proofMintingPolicyContractApplied,
    writeToFile,
) where

import PlutusTx (
    CompiledCode,
 )
import Prelude (
    IO,
    error,
    pure,
 )

verifyAppliedCompiled :: CompiledCode ()
verifyAppliedCompiled =
    error "Axiom SHPLONK generation uses Generic.VerificationTestPlutus directly"

proofMintingPolicyContractApplied ::
    CompiledCode ()
proofMintingPolicyContractApplied =
    error "Axiom SHPLONK generation uses Generic.VerificationTestPlutus directly"

writeToFile :: IO ()
writeToFile =
    pure ()
